import React from 'react';
import { PlotFormComp } from '../../components/plot-form';

const Home = () => (
  <div className="wrapper">
    <h2>Teams plot download</h2>
    <p>Only available in PDF format</p>
    <PlotFormComp />
  </div>
);

export default Home;
